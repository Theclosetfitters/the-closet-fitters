# components/3d

All Three.js / React Three Fiber components for the live closet render.

Rules (see root `CLAUDE.md`):

- `ClosetViewer` takes the configuration as **props** and is fully controlled —
  it holds no internal state for selections.
- These are Client Components (`"use client"`), since R3F needs the browser.
