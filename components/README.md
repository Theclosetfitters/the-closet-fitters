# components

Reusable UI components (buttons, forms, layout, configurator panels, order cards).

- Keep presentational components free of data-fetching where practical.
- 3D / React Three Fiber components live in [`./3d`](./3d).
